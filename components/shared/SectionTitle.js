import React from "react";

const SectionTitle = ({ text }) => {
  return (
    <div className="mt-4 mb-4 text-center ">
      <h2 className="color-secondary  font-weight-bold ">{text}</h2>
    </div>
  );
};

export default SectionTitle;
